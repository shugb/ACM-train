//#define LOCAL
#include <set>
#include <map>
#include <list>
#include <queue>
#include <vector>
#include <cstdio>
#include <string>
#include <cstring>
#include <iomanip>
#include <iostream>
#include <sstream>
#include <algorithm>
#include <math.h>
#include <cmath>
#define eps 1e-7
#define INF 1e9+7
using namespace std;
int edge[1005][1005];//?????????????
int book[1005];//???????????
int dis[1005];//???????
int num=0;//?????????

int prim(int s) {
    int pos, min;//pos????????????????????book????????min??????????????????????
    int MST = 0;//????????????????????????

    book[s] = 1;//??????????s??????book
    pos = s;

    for (int i = 1; i <= num; i++) //?????dis
        dis[i] = edge[pos][i];
      cout<<s<<"->";
    //???n-1??
    for (int i = 2; i <= num; i++) {
        min = INT_MAX;

        for (int j = 1; j <= num; j++) {
            if (book[j]==0 && min > dis[j]) {
                min = dis[j];
                pos = j;
            }
        }
        book[pos] = 1;//????????????????????????
        MST += min;//???????????????
        if(i!=num)
        cout<<pos<<"->";
        else
            cout<<pos;
        for (int j = 1; j <= num; j++) //???????dis
        if (book[j]==0 && dis[j] > edge[pos][j])//??????????j??????????????????????????j??????????
            dis[j] = edge[pos][j];
    }
    return MST;
}

int main()
{
    #ifdef LOCAL
    freopen("in.txt", "r", stdin);
    freopen("out.txt", "w", stdout);
    #endif // LOCAL
    int n;
    int i,j;
    while(cin>>n)
{
        for(int k=1;k<=n;k++) {
            book[k]=0;
            dis[k]=0;
            for(int t=1;t<=n;t++) {
                edge[k][t]=0;
            }
        }
        num=n;
        for(i=1;i<=n;i++)//?????????????
            for(j=1;j<=n;j++)
                cin>>edge[i][j];
        int ans = prim(1);
        cout<<endl;
        cout << ans << endl;
}
    return 0;
}
